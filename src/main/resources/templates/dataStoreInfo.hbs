<bean id="dataStoreFactory" class="java.util.HashMap">
    <constructor-arg>
        {{#each entities}}
            <entry key="{{name}}" value="{{keyField}}"></entry>
        {{/each}}
    </constructor-arg>
</bean>