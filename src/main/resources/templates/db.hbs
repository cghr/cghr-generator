{{~#each entities ~}}
#Table:{{name}}
DROP TABLE IF EXISTS `{{name}}`;
CREATE TABLE `{{name}}`(
{{~#each properties ~}}
    {{~#unless @last ~}}
        {{name}} {{type}},
    {{~else ~}}
        {{name}} {{type}}
    {{~/unless ~}}
{{~/each~}}
);
{{~/each ~}}
